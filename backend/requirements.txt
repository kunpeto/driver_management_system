# ============================================================
# 司機員管理系統 - 雲端後端 API (Render FastAPI)
# ============================================================

# 資料庫連線
pymysql==1.1.2
SQLAlchemy==2.0.46
cryptography>=42.0.0  # pymysql SSL 支援

# FastAPI 後端
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# 認證與安全
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.22
bcrypt==5.0.0

# 定時任務
APScheduler==3.11.2

# Google API（Sheets 同步）
google-auth==2.48.0
google-auth-oauthlib==1.2.4
google-auth-httplib2==0.3.0
google-api-python-client==2.188.0

# HTTP 請求
httpx==0.28.1

# 日誌
loguru==0.7.3

# ============================================================
# 注意：不包含 PyQt6、PDF 處理等桌面應用套件
# 這些套件僅用於本機 desktop_app
# ============================================================
